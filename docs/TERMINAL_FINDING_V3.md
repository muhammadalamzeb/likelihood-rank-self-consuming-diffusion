# Terminal finding under Constraint Set v3 (Path 1)

**Date:** 2026-09-23  
**Status:** Path-1 topic search **exhausted for practical KEEP** under active v3 rules.  
**Publication:** **blocked** (no KEEP / strong MODIFY with experiment plan).

## What this is

A process terminal for **Strategy B + top-down discovery under Constraint Set v3**, not a claim that GenAI research is impossible.  
`TERMINAL_FINDING_V2.md` remains valid under **v2** only.

## Evidence summary

| Track | Outcome |
|-------|---------|
| v3 lit rounds 1–4 | V3-1…V3-20 **KILL** |
| V3-21 meta Path-3 | **KILL** (AI-Scientist / AutoResearch failure lit) |
| Strategy B stacks v0–v3 (pre-v3 / carried) | FC*, FM1, T3, D1 **KILL** |
| Stacks v4–v8 | GraphVAE, digits erase/τ/N, GMM imbalance, synth-spec SNR/jitter, SEM–ATE **KILL** or no-claim |

**KEEP survivors: none.**  
Hard exclusions **1–39** still in force; proposed **40–49** listed in `DISCOVERY_V3.md` / `KILL_LOG.md` (owner approval pending).

## Interpretation

Under v3’s L2 bar (no replication / benchmark / cosmetic ablation; prefer new intervening variables; kill before lock), both:

1. literature-first candidate waves, and  
2. observation-first small runnable stacks  

repeatedly rediscover **named** neighborhoods. That is a property of the **search policy + novelty bar**, not a proof of global GenAI saturation.

## What is *not* authorized by this file

- Drafting a venue paper from killed clusters  
- Fabricating results or citations  
- Silently relaxing v3 (Path **2** requires explicit `CONSTRAINTS.md` override)

## Owner forks (historical — Path 4 executed)

See `PUBLICATION_STATUS.md` / `PATH4_ARCHIVE.md`:

1. ~~Continue Path 1~~ — closed  
2. ~~Explicit constraint relaxation~~ — not taken for Path 4  
3. ~~Meta/process paper sharper than V3-21~~ — not taken (V3-21 KILL)  
4. **Stop — archive + kill-log as deliverable** — **DONE** (`PATH4_ARCHIVE.md`)

## Artifacts

- `docs/DISCOVERY_V3.md`, `docs/KILL_LOG.md`, `docs/CONSTRAINTS.md`  
- `docs/PATH2_DISCOVERY.md`, `docs/PATH3_CAUSAL_BOUNDARY.md`, `docs/PATH4_ARCHIVE.md`  
- `experiments/logs/*`, `experiments/scripts/*`  
- `docs/STRATEGY_B.md`
